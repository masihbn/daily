# Local Claude Code MCP config — points at this machine's Supabase project setup,
# not app code. Not needed to run or deploy the PWA.
.mcp.json

node_modules/
test-results/
playwright-report/
blob-report/
.playwright/
